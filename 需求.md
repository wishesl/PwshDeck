# 需求文档：pwsh 桌面交互式终端

## 1. 是什么 (What)
构建一个基于 Wails v3 的 PowerShell (pwsh) 桌面端应用程序。
核心目标是实现一个**交互式终端**，允许用户在 GUI 界面输入 PowerShell 命令，并实时查看执行结果。

## 2. 为什么 (Why)
- 提供一个便捷的、可定制的 PowerShell 图形界面。
- 将 pwsh 的强大能力与现代桌面 UI 结合，为后续功能（如脚本管理、AI 辅助、MCP 集成）打下基础。

## 3. 怎么做 (How) - 第一阶段实现链路
本阶段聚焦于打通“启动 $\rightarrow$ 执行 $\rightarrow$ 渲染”的最简闭环。

### 核心实现路径：
1. **后端 (Go)**:
   - 创建 `PwshService`。
   - 使用 `os/exec` 启动 `pwsh.exe` 进程。
   - 通过管道 (Pipe) 实时捕获 `stdout` 和 `stderr`。
   - 使用 `Wails Event` 将捕获的输出实时推送到前端。
   - 提供 `ExecuteCommand(cmd string)` 方法，通过 `stdin` 向 pwsh 进程发送命令。

2. **前端 (React + TS)**:
   - 创建一个简单的终端界面（输入框 + 输出区域）。
   - 监听 Wails 实时事件，将接收到的文本追加到输出区域。
   - 实现基本的 ANSI 颜色渲染（确保 pwsh 的彩色输出能正确显示）。

### 技术难点 [难点]:
- **实时流输出**：避免阻塞，确保输出是逐行或逐字实时推送，而非等待进程结束。
- **ANSI 渲染**：pwsh 输出包含 ANSI 转义序列，前端需要解析这些序列以显示颜色。
- **进程生命周期**：管理 pwsh 进程的启动、维持和优雅关闭。
